---
/**
 * EndOrnament - A handmade, paper-like SVG illustration
 * placed at the end of the home page. Replaces the heavy
 * canvas ExplorerAnimation with an elegant, artisanal touch.
 */
import { Image } from "astro:assets";
import surrealHands from "../assets/surreal-hands-ornament.png";
---

<div class="end-ornament" aria-hidden="true">
  <Image
    src={surrealHands}
    alt="Surreal End Illustration"
    class="ornament-image"
  />
</div>

<style>
  .end-ornament {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: var(--space-y-xl);
    padding: var(--space-8) 0;
    width: 100%;

    /* 
      Light Mode Blending: 
      The image has a pure white background and dark graphite lines.
      Multiply blends the white background perfectly into the page's cream background, leaving only the dark lines visible. 
    */
    mix-blend-mode: multiply;
    opacity: 0.85; /* Soften the graphite slightly so it doesn't contrast too harshly */
  }

  .ornament-image {
    width: calc(
      var(--space-100) * 3
    ); /* Scale using geometric spacing tokens */
    height: auto;
    max-width: 100%;
  }

  /* 
    Dark Mode Blending:
    The page background is deep espresso void.
    Inverting the image turns the white background black and the dark lines white.
    Screen blending makes the black background completely transparent against the page, leaving only glowing white lines.
  */
  :global([data-theme="dark"]) .end-ornament {
    mix-blend-mode: screen;
    filter: invert(1) hue-rotate(180deg);
    opacity: 0.65; /* The glowing white lines need to be softened against the dark void */
  }

  @media (max-width: 600px) {
    /* @breakpoint-mobile */
    .end-ornament {
      margin-top: var(--space-y-md);
      padding: var(--space-4) 0;
    }

    .ornament-image {
      width: calc(var(--space-100) * 2.2);
    }
  }
</style>
