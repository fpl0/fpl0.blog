---
/**
 * EndOrnament - A handmade, paper-like illustration
 * placed at the end of the home page.
 * Theme: A vintage, Da Vinci-style graphite portrait of the author.
 */
import { Image } from "astro:assets";
import authorSketch from "../assets/fpl0-sketch.png";
---

<div class="end-ornament" aria-hidden="true">
  <Image
    src={authorSketch}
    alt="Vintage graphite sketch of the author"
    class="ornament-image"
    width={512}
  />
</div>

<style>
  .end-ornament {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: var(--space-y-xl);
    padding: var(--space-8) 0;
    width: 100%;
    opacity: 0.9;
  }

  /* The sketch is generated on a pure white background.
     In light mode, `mix-blend-mode: multiply` makes the white entirely transparent,
     letting the warm paper background bleed through.
     We then use sepia/opacity to soften the black graphite to match `--color-text-muted`. */
  .ornament-image {
    /* Scale proportionally to the site header logo */
    height: calc(var(--font-size-h2) * 6);
    width: auto;
    object-fit: contain;

    mix-blend-mode: multiply;
    filter: sepia(0.2) opacity(0.75);
    transition:
      filter var(--duration-slow) var(--ease-out),
      opacity var(--duration-slow) var(--ease-out);
  }

  /* In dark mode, we must mathematically reverse the image.
     1. invert(1) turns the white bg to black, and the black pencil to white.
     2. mix-blend-mode: screen; makes the black bg entirely transparent.
     3. We then apply heavy sepia and hue-rotate to tint the white pencil lines 
        into our elegant "Aged Gold Leaf" primary color (approx hue 75). */
  :global([data-theme="dark"]) .ornament-image {
    mix-blend-mode: screen;
    filter: invert(1) sepia(1) saturate(1.5) hue-rotate(30deg) brightness(0.9)
      opacity(0.8);
  }

  @media (max-width: 600px) {
    /* @breakpoint-mobile */
    .end-ornament {
      margin-top: var(--space-y-md);
      padding: var(--space-4) 0;
    }

    .ornament-image {
      height: calc(var(--font-size-h2) * 4);
    }
  }
</style>
