/**
 * Shared constants for content scripts and schema validation.
 */

/** Minimum character count for content summaries. */
export const SUMMARY_MIN = 50;

/** Maximum character count for content summaries. */
export const SUMMARY_MAX = 360;
